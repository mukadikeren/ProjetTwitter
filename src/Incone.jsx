import { Children } from "react";

function Incone(props) {

    return(
        <div>
        <img src={props.image} />
        {/* <span>{props.children}</span> */}
      </div>
    )
}

export default Incone;
